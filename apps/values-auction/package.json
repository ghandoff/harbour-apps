{
  "name": "values-auction",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "values auction — a live, facilitator-driven classroom game for the winded.vertigo / PRME network",
  "scripts": {
    "dev": "vite",
    "dev:server": "tsx watch server/index.ts",
    "build": "vite build",
    "build:prod": "VA_BASE=/harbour/values-auction/ vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "lint": "eslint",
    "wrangler:dev": "wrangler dev",
    "deploy": "npm run build:prod && wrangler deploy",
    "deploy:assets-only": "npm run build:prod && wrangler deploy --compatibility-date 2025-04-01"
  },
  "dependencies": {
    "@fontsource/inter": "^5.1.0",
    "@resvg/resvg-js": "^2.6.2",
    "html-to-image": "^1.11.11",
    "lit": "^3.2.1",
    "satori": "^0.10.14",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.9.1",
    "@types/ws": "^8.5.13",
    "jsdom": "^25.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
